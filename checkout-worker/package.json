{
  "name": "5metri-checkout-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "wrangler deploy --dry-run --outdir .wrangler-build",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.123.0"
  }
}
